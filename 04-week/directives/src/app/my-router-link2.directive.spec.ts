import { MyRouterLink2Directive } from './my-router-link2.directive';

describe('MyRouterLink2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyRouterLink2Directive();
    expect(directive).toBeTruthy();
  });
});
